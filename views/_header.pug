header.header
    nav.nav.nav--course
        a.nav__el(href='/') Loans
        
    nav.nav.nav--user
        a.nav__el.nav__el(href='/login') Login
        a.nav__el.nav__el(href='/signUp') Sign Up
        if authenticated === true
            a.nav__el
                span #{user.firstname} 